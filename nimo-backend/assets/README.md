# Place tray/window icons here
#
#   icon.png   – cross-platform PNG (256×256 minimum)
#   icon.ico   – Windows installer + tray
#   icon.icns  – macOS .dmg + tray
#
# If no icon is provided, the tray falls back to a tiny built-in PNG
# buffer so the app still boots.
#
# Recommended source: a single PNG exported from Figma, plus online
# converters for .ico and .icns (e.g. icoconvert.com, cloudconvert).
